services:
  server_a:
    build:
      context: .              # gRPC_practive/ 為 root，讓 Dockerfile 能存取 proto/
      dockerfile: server_A/Dockerfile
    ports:
      - "50051:50051"
    environment:
      - PYTHONUNBUFFERED=1
