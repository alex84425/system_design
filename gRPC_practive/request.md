我要練習使用 gRPC
code 都寫在 qRPC_practive/

1. use docker
2. docker have simeple server_A using gRPC
3. server_A using python and uv and gRPC package, have simple end point


1. 我預期 grpc_practive-server_a 有個endpoint
2. 我用 client.py 去 call server_A 的 endpoint
3. client.py代表前端 在我Local 可以執行 也幫local 裝 UV

